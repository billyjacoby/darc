import { ArrowRight } from "lucide-react";

export const TabSwitchShortcut = () => {
	return (
		<span className="flex items-center gap-2">
			<span>Switch to tab</span>
			<ArrowRight size={16} />
		</span>
	);
};
